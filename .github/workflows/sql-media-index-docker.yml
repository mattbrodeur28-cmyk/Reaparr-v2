name: Build SQL Media Index Docker

on:
  push:
    branches:
      - feature/sql-media-index
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Reaparr SQL Media Index
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ghcr.io/mattbrodeur28-cmyk/reaparr-v2:sql-media-index
          build-args: |
            VERSION=1.0.0-sql-media-index.${{ github.run_number }}
            INFORMATIONAL_VERSION=${{ github.sha }}
            BUILDPLATFORM=linux/amd64
            TARGETPLATFORM=linux/amd64
